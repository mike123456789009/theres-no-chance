name: baseline
description: Baseline replay using recorded liquidity and default strategy toggles.
dataset: artifacts/datasets/backtests/demo_baseline.jsonl
adjustments:
  microstructure_penalty_add: 0.0
  slippage_bps: 0.0
  liquidity_scalar: 1.0
  spread_multiplier: 1.0
  scenario_tags:
    - baseline
overrides:
  strategy:
    enabled: true
    packs:
      - core
